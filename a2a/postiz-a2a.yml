generated: '2026-08-13'
method: probed
source: https://docs.postiz.com/.well-known/agent-card.json
discovery:
  path: /.well-known/agent-card.json
  canonical: true
  host: docs.postiz.com
  note: >-
    Served from Postiz's own documentation host. The card names itself "Postiz
    Documentation" and its provider organization is Postiz Documentation, so the
    document belongs to this company. api.postiz.com and postiz.com both returned
    404 for both the canonical and the legacy /.well-known/agent.json path.
conformance:
  spec: A2A 1.0.0
  grade: conformant
  protocol_version: '0.3'
  preferred_transport: HTTP+JSON
  hard_checks:
    capabilities_is_object: true
    protocol_version_present: true
    skills_is_array: true
  optional_fields:
    preferred_transport: true
    default_input_modes: true
    default_output_modes: true
  deviations:
  - supportedInterfaces-instead-of-additionalInterfaces
  deviation_notes: >-
    The card carries `supportedInterfaces` rather than the A2A 1.0.0
    `additionalInterfaces` field. Every hard conformance check still passes
    (capabilities is an object, protocolVersion is present, skills is an array)
    and all three optional fields are present, so the card grades conformant with
    that one naming deviation recorded.
card:
  name: Postiz Documentation
  url: https://docs.postiz.com/
  version: 1.0.0
  organization: Postiz Documentation
  documentation_url: https://docs.postiz.com/
  capabilities:
    streaming: false
    push_notifications: false
  default_input_modes: [text/plain]
  default_output_modes: [text/plain]
  skills: 1
  skill_ids: [postiz]
  skill_urls:
  - https://docs.postiz.com/.well-known/agent-skills/postiz/skill.md
  file: postiz-agent-card.json
related:
  agent_skill: skills/postiz-provider-skill.md
  mcp_server: mcp/postiz-mcp.yml
  note: >-
    The card's single skill points at a provider-published Agent Skill markdown file,
    not at an A2A or MCP endpoint. Postiz's actual agent execution surface is the
    hosted MCP server at https://api.postiz.com/mcp, captured separately.
x-evidence:
  fetched: '2026-08-13'
  url: https://docs.postiz.com/.well-known/agent-card.json
  http_status: 200
  content_type: application/json
maintainers:
- FN: Kin Lane
  email: kin@apievangelist.com
